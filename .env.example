# Airflow K3s Deployment Configuration

# Kubernetes
AIRFLOW_NAMESPACE=airflow
KUBECONFIG=${HOME}/.kube/config

# Airflow Version
AIRFLOW_VERSION=3.1.6

# Storage - volumes directory inside the repo (created automatically)
# Leave empty to use default: ${PROJECT_ROOT}/volumes
VOLUMES_PATH=

# Networking
WEBSERVER_PORT=30123

# Secrets (auto-generated if empty)
FERNET_KEY=
WEBSERVER_SECRET_KEY=

# Database
POSTGRES_PASSWORD=alphabet_50UP

# Git Sync (optional - leave empty to use local dags/ directory)
GIT_SYNC_REPO=
GIT_SYNC_BRANCH=main
GIT_SYNC_SUBPATH=dags

# Resource Limits (adjust for your server)
WEBSERVER_MEMORY_REQUEST=2Gi
WEBSERVER_MEMORY_LIMIT=4Gi
SCHEDULER_MEMORY_REQUEST=2Gi
SCHEDULER_MEMORY_LIMIT=4Gi
